class Rect {
    constructor(x, y, width, height) {
        this.x = x;
        this.y = y;
        this.width = width;
        this.height = height;
    }

    get Cordinates() {
        return (arr[x,y]);
    }

    get Size() {
        return (arr[width,height])
    }
}