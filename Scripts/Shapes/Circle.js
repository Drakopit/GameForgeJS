class Circle {
    constructor(x, y, r) {
        this.x = x;
        this.y = y;
        this.r = r;
    }

    get Cordinates() {
        return (arr[x,y]);
    }

    get Size() {
        return (Math.PI * (this.r*this.r))
    }
}